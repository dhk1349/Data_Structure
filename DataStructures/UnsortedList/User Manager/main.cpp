#include "Cmd.h"
using namespace std;

int main() {
		/*
		ArrayList tmp;
		ItemType item;
		item.SetId(0);
		item.SetAddress("∞Ê±‚");
		item.SetName("dhk1349");
		tmp.Add(item);
		//tmp.PrintAll();

		item.SetId(1);
		item.SetAddress("±§¡÷");
		item.SetName("DEvT");
		tmp.Add(item);
		tmp.PrintAll();
		*/
	Cmd controller;
	controller.run();
	return 1;
}